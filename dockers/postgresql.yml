postgresql:
  environment:
    POSTGRES_DB: postgres
    POSTGRES_PASSWORD: postgres
    POSTGRES_USER: postgres
  image: postgres:9.6
  ports:
    - "5432:5432"
  volumes:
    - /var/lib/postgresql/data